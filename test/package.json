{
  "name": "brackets-tests",
  "private": true,
  "type": "module",
  "description": "Tests for /brackets. The app itself has no dependencies and no build step; these are the only node_modules in the repository, and they are dev-only.",
  "scripts": {
    "test": "node run.mjs",
    "setup": "npm install && npx playwright install chromium"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "axe-core": "^4.13.0",
    "playwright": "^1.63.0"
  }
}
